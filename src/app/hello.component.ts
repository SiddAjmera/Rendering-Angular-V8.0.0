import { Component, Input } from '@angular/core';

@Component({
  selector: 'child-cmp',
  template: `<p>This is Child Cmp!</p>`,
  styles: [`h1 { font-family: Lato; }`]
})
export class HelloComponent  {
}
